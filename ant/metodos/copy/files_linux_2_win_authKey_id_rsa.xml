<project name="files.linux.2.win.authKey.id.rsa" default="files.linux.2.win.authKey.id.rsa" basedir=".">

	<target name="files.linux.2.win.authKey.id.rsa">
	
		<mkdir dir="${var5}"/>
	
		<scp file="${var6}@${var1}:${var3}${var4}" 
			 todir="${var5}" 
			 trust="true"
			 keyfile="${user.home}\.ssh\id_rsa"
		/>

		<mkdir dir="${var5}"/>
		
	</target>
	
</project>