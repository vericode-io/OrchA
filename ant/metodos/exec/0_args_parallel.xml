<project name="0.args_parallel" default="0.args_parallel" basedir=".">
	
	<target name="0.args_parallel">
	
		<exec executable="${basedir}\metodos\exec\0_args_parallel.bat">
		<arg value="${var3}"/>
		<arg value="${var1}"/>
		<arg value="${var4}"/>
		</exec>
	
	</target>
		
</project>