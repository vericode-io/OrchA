<project name="2.args" default="2.args" basedir=".">
	
	<target name="2.args">
	
		<exec executable="${basedir}\metodos\exec\2_args.bat">
		<arg value="${var3}"/>
		<arg value="${var1}"/>
		<arg value="${var4}"/>
		<arg value="${var5}"/>
		<arg value="${var6}"/>
		</exec>
	
	</target>
		
</project>