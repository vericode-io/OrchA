<project name="process.enable_net_interface" default="process.enable_net_interface" basedir=".">
	
	<target name="process.enable_net_interface">
	
		<exec executable="${basedir}\metodos\process\enable_net_interface.bat" failifexecutionfails="false">
		<arg value="${var1}"/>
		<arg value="${var3}"/>
		</exec>
	
	</target>
		
</project>